%h1 Категории

= link_to 'Добавить категорию', new_category_path, class: "btn btn-warning"

%table.table.table-striped.table-hover
  %thead
    %tr
      %th Краткое наименование категории
      %th Полное наименование категории
      %th Количество дней отпуска
      %th
      %th
      %th

  %tbody
    - @categories.each do |category|
      %tr
        %td= category.name_c
        %td= category.full_name_c
        %td= category.d_vocation_c
        %td= link_to 'Показать', category, class: "btn btn-warning"
        %td= link_to 'Редактировать', edit_category_path(category), class: "btn btn-default "
        %td= link_to 'Удалить', category, method: :delete, data: { confirm: 'Вы уверены?' }, class: "btn btn-danger"

%br
